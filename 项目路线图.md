# 项目路线图

本文档承接第一轮“策划者 + 并行代理”的产出，用来保持项目主线清晰：先把数据和回测地基打稳，再逐步实现趋势判断、价格行为、量价确认和风控执行。

## 今日复盘：2026-05-13

今天完成的是项目从“策略想法”到“可工程化推进框架”的第一步。

主要进度：

- 固化三周期回测分层：A 日内快节奏、B 标准波段、C 慢趋势。
- 建立周期组评分与淘汰规则：重点验证 B，A 受交易成本约束，C 受交易样本量约束。
- 接入 OKX CLI 公开行情读取，完成 ticker 和 candles 的解析测试。
- 建立轻量历史 K 线仓库，先锁定去重、排序、最新时间戳和所需 bar 推导接口。
- 建立市场体制基础指标，覆盖 True Range、Kaufman ER、CHOP 和 regime 分类。
- 建立“策划者 + 并行代理”协作流程，完成第一轮研究和执行任务闭环。
- 更新 README，让当前项目结构可以被后续上下文直接接手。

今日判断：

- 当前路线不需要急着进入复杂价格行为识别。先把行情数据、无前瞻回测、成本模型和风控模型做稳，是更高胜率的工程顺序。
- 暂时不应接入真实交易权限，也不应把 OKX API secret、passphrase 等敏感信息写入项目文件或聊天记录。
- 当前内存版 `CandleRepository` 只是接口地基，不是最终历史数据仓库。后续需要迁移到 DuckDB/Parquet。

## 当前状态

已完成：

- 三周期回测分层：A 日内快节奏、B 标准波段、C 慢趋势。
- 周期组排序规则：B 优先，A 受手续费占毛利比例约束，C 受样本数量约束。
- OKX CLI 公开行情适配器：ticker 和 candles 解析。
- 历史 K 线轻量仓库：去重、排序、最新时间戳、所需 bar 推导。
- 市场体制基础指标：True Range、Kaufman ER、CHOP、体制分类。
- 并行代理协作流程：策划者、研究代理、执行代理的分工和验收规则。

## 数据地基 v1：2026-05-13 推进批次

本批次目标是把“OKX 主源 + Binance 校验源”的数据地基落到代码层，暂不进入实盘、回测撮合或复杂策略信号。

已落地：

- OKX 适配器支持 candles 历史分页参数 `after/before`。
- OKX 适配器支持 instruments 元数据读取。
- OKX CLI 路径支持通过显式参数或 `OKX_CLI_COMMAND` 环境变量配置。
- 新增资产宇宙模块，固化 `BTC-USDT`、`ETH-USDT`、`XAUT-USDT`。
- 新增 canonical symbol 映射，区分 `BTC/USDT`、OKX `BTC-USDT`、Binance `BTCUSDT`。
- 明确主数据源为 `okx`，预留校验数据源为 `binance`。
- 新增 DuckDB 历史行情仓库，使用 `venue, inst_id, inst_type, bar, ts_ms` 作为去重边界。
- 新增 download_state 读写，按 `venue, inst_type, inst_id, bar` 隔离下载状态。
- 新增本地 `.venv` 依赖方式，`duckdb` 只安装到项目目录，不污染全局 Python。

关键边界：

- 不把 OKX 和 Binance K 线混成综合成交价格。
- 回测成交价格必须绑定具体 `venue`。
- Binance 本轮只作为 BTC/ETH 校验源预留，尚未实现 adapter。
- XAUT 是否适合 Binance 校验需要单独核对，不能默认等同 OKX 的 `XAUT-USDT`。

## 本地环境与存储边界

项目根目录固定为 `D:\交易系统`。

后续规则：

- 项目独有依赖、下载的数据、临时数据库、回测结果、报告输出都优先放在 `D:\交易系统` 下。
- 如需建立虚拟环境，默认放在 `D:\交易系统\.venv`。
- 如需存储历史行情，默认放在 `D:\交易系统\data` 或 `D:\交易系统\storage`。
- 如需保存回测结果，默认放在 `D:\交易系统\reports` 或 `D:\交易系统\backtest_results`。
- 非必要不向 C 盘写入大文件；全局 CLI 工具除外，但项目数据不得依赖 C 盘空间。
- 不把 API key、secret key、passphrase 写入仓库文件。未来若需要认证，只使用本机安全配置或环境变量，并保持与回测代码隔离。

## 模型使用约定

后续协作默认采用：

- 主线程策划者：`gpt-5.5`，超高推理强度。
- 执行代理：`gpt-5.5`，高推理强度。

除非用户明确要求，否则后续不会为了普通小任务派发代理；只有任务可以并行拆分、且写入范围明确时才使用执行代理。

## 第一轮代理结论

### OKX 数据架构

首版只做公开行情，不接真实交易权限。三资产使用 `BTC-USDT`、`ETH-USDT`、`XAUT-USDT` 现货 K 线。三周期并集需要 `5m`、`15m`、`1H`、`4H`、`1D`。

关键要求：

- 只使用 `confirm=1` 的已完成 K 线进入回测。
- OKX K 线 `ts` 视为开盘时间，信号最早只能在该 K 线收盘后使用。
- 多周期对齐时，入场周期不能读取尚未收盘的结构周期或趋势周期。
- XAUT 现货不要和 XAU 衍生品混用。
- funding rate 和 open interest 属于 SWAP 扩展数据，不能混进现货 K 线主表。

推荐下一步：

- 扩展 `okx_cli.py` 支持 `after/before` 和 `instruments`。
- 新增 `data/universe.py` 管理资产和 OKX bar 映射。
- 新增 DuckDB 历史存储，实现 candles 表和 download_state。
- 新增下载器，负责分页、重试、confirm 过滤和增量同步。

### 指标层

已落地市场体制最小版本。当前指标可以先用于趋势周期过滤，后续再加入 ADX、EMA50/200、BOS 方向、FVG、OB、流动性池和量价结构。

推荐下一步：

- 加入 EMA 和 ADX。
- 定义趋势周期的 regime 输出对象。
- 为后续价格行为模块预留结构引用，例如前高前低、扫荡区、FVG 区间。

### 历史行情仓库

已落地内存版 `CandleRepository`，用于锁定接口和测试行为。它不是最终存储方案。

推荐下一步：

- 保留当前接口，新增 DuckDB 实现。
- candles 表使用唯一键 `venue, inst_id, inst_type, bar, ts_ms`。
- download_state 记录每个 `inst_id + bar` 的最新/最早时间戳、行数、错误和 CLI 版本。

### 回测与风控

第一版不应急着实现复杂 OB/FVG，而是先搭建无前瞻偏差的撮合、成本、仓位和交易日志框架。

推荐核心对象：

- `Signal`：信号方向、产生时间、可执行时间、入场价、止损、目标、趋势和结构上下文。
- `OrderIntent` / `OrderEvent`：模拟订单意图和成交事件。
- `CostModel`：手续费、点差、滑点、资金费率。
- `PositionState`：虚拟持仓、仓位、止损、R 倍数、已实现和未实现盈亏。
- `TradeLog`：入场、部分止盈、移动止损、最终出场、费用和 R multiple。

风控默认值：

- 单笔风险先用权益的 `0.5%`。
- 1R 先平 50%，剩余仓位移动到覆盖成本后的保本。
- 日内最大回撤先设 `2%`。
- 总回撤熔断先设 `5%`。
- 第一阶段全部为模拟盘规则，不触发真实下单。

## 优化后的下一轮执行顺序

优化原则：先让“数据 -> 回测 -> 成本 -> 风控”闭环可跑，再逐步丰富趋势、价格行为和量价确认。这样可以尽早发现前瞻偏差、成本吞噬、样本不足和周期选择错误。

### 任务 1：扩展 OKX CLI 数据适配器

写入范围：

- `trading_system/data/okx_cli.py`
- `tests/test_okx_cli_market_data.py`

验收：

- `get_candles(...)` 支持 `after` 和 `before` 可选参数。
- 新增 `Instrument` dataclass。
- 新增 `get_instruments(inst_type, inst_id=None)`。
- 单元测试通过 runner 注入验证命令参数和解析结果。

### 任务 2：新增资产宇宙模块

写入范围：

- `trading_system/data/universe.py`
- `tests/test_data_universe.py`

验收：

- 固化 `BTC-USDT`、`ETH-USDT`、`XAUT-USDT`。
- 从默认 profile 推导 OKX bar 并集：`5m, 15m, 1H, 4H, 1D`。
- 明确 `1h -> 1H`、`4h -> 4H`、`1d -> 1D` 的映射。

### 任务 3：升级历史行情存储

写入范围：

- `trading_system/data/history.py`
- `tests/test_data_history.py`

验收：

- 保留当前内存仓库测试。
- 新增 DuckDB 仓库设计或最小实现。
- candles 表支持唯一键去重、时间范围读取、最新/最早时间戳查询。
- download_state 能记录每个 `inst_id + bar` 的下载状态。

### 任务 4：定义回测和风控数据模型

写入范围：

- `trading_system/backtest/models.py`
- `trading_system/backtest/__init__.py`
- `trading_system/risk/models.py`
- `trading_system/risk/__init__.py`
- `tests/test_backtest_models.py`
- `tests/test_risk_models.py`

验收：

- 定义 Signal、OrderIntent、OrderEvent、PositionState、TradeLog。
- 模型必须包含时间字段，明确 `generated_at_ms` 和 `valid_from_ms`。
- 测试覆盖信号不能在生成 K 线未收盘前被执行。

### 任务 5：实现成本模型

写入范围：

- `trading_system/backtest/costs.py`
- `tests/test_backtest_costs.py`

验收：

- 支持手续费、点差、滑点、资金费率字段。
- 能输出单笔交易总成本。
- 能计算手续费占毛利比例，供 A 组淘汰规则复用。

### 任务 6：补充趋势指标层

写入范围：

- `trading_system/indicators/*`
- `tests/test_regime_indicators.py`

验收：

- 新增 EMA50/200 或通用 EMA。
- 新增 ADX 或先定义 ADX 接口与测试样例。
- 输出趋势周期的结构化 regime 对象，为后续策略信号生成提供稳定输入。

## 策划者审核标准

每轮结束都必须回答：

- 新增了什么？
- 验证了什么？
- 是否引入真实交易风险？
- 是否存在前瞻偏差风险？
- 下一轮最小可交付任务是什么？

在完整测试通过前，不把任何模块标记为完成。
