# 三周期交易策略回测分层

本文件固化第一版回测周期分层规则。代码实现位于 `trading_system/timeframe_profiles.py`，测试位于 `tests/test_timeframe_profiles.py`。

## 周期分组

| 组别 | 入场周期 | 结构周期 | 趋势周期 | 用途 | 默认排序 |
| --- | --- | --- | --- | --- | --- |
| B 标准波段 | 15m | 1h | 4h | BTC/ETH/XAUT 主回测组，兼顾信号质量和交易频率 | 1 |
| C 慢趋势 | 1h | 4h | 1d | 低频趋势捕捉，尤其适合 XAUT 和大级别 BTC/ETH | 2 |
| A 日内快节奏 | 5m | 15m | 1h | BTC/ETH 高流动性时段的扫荡、OB/FVG 回踩 | 3 |

不使用 `1m` 作为主策略周期，避免手续费、滑点和假突破噪音吞噬毛利。

## 回测输出指标

每个周期组必须输出：

- `net_profit`
- `max_drawdown`
- `win_rate`
- `profit_loss_ratio`
- `profit_factor`
- `average_holding_time`
- `expectancy_per_trade`
- `fee_to_gross_profit_ratio`

## 判定规则

- A 组若 `fee_to_gross_profit_ratio > 25%`，直接标记为 `rejected`。
- C 组若交易次数低于最小样本数，默认 `30`，标记为 `supporting_only`，只作为趋势过滤或辅助模块。
- B 组默认是主策略候选；最终排序仍会结合净利润、回撤、Profit Factor 和单笔期望。

## 代码用法

```python
from trading_system.timeframe_profiles import BacktestResultSummary, rank_profile_results

ranked = rank_profile_results([
    BacktestResultSummary("A", "BTC/USDT", gross_profit=3000, net_profit=2100, total_fees=900, trade_count=500),
    BacktestResultSummary("B", "BTC/USDT", gross_profit=2000, net_profit=1400, total_fees=120, trade_count=80),
    BacktestResultSummary("C", "BTC/USDT", gross_profit=2400, net_profit=1800, total_fees=80, trade_count=10),
])

best = ranked[0]
```

默认情况下，上例会选择 B 组作为最优主策略候选，C 组因样本不足降级，A 组因成本过高淘汰。
