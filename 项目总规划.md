# 项目总规划

本文档是交易系统的主规划文档，记录最终愿景、阶段路线、当前完成度、下一小计划和风险边界。后续每个阶段完成后，优先更新本文档。

## 最终愿景

构建一套可长期运行的量化交易研究与执行辅助系统，用确定性规则完成交易热路径，用 Agent 做慢速解释、复盘和优化建议。

核心链路：

```text
趋势判断 -> 价格行为 -> 量价确认 -> 风控执行 -> 复盘进化
```

第一阶段不接入真实资金自动下单。系统先完成数据、回测、看板、模拟盘和解释闭环，再进入小资金实盘准备，最后才部署到云端 Hermes Agent。

## 支持范围

第一版资产：

- `BTC/USDT`
- `ETH/USDT`
- `XAUT/USDT`

第一版运行模式：

- 历史行情下载与数据质量检查。
- 三周期并行回测。
- 本地 Streamlit 网页看板。
- 模拟盘虚拟订单和虚拟持仓。
- 交易日志、解释、复盘、优化建议。

暂不支持：

- 未经人工审批的真实资金自动下单。
- 高杠杆合约实盘。
- 期权、跨所搬砖、复杂套利。
- 云端 24 小时部署前的无人值守实盘。

## 阶段路线

### P0 项目地基

目标：让项目可维护、可回滚、可验证。

内容：

- Git 版本控制。
- 本地 `.venv`。
- `requirements.txt`。
- 测试体系。
- D 盘项目目录和存储边界。
- 四份核心文档体系。

状态：进行中，文档精简批次完成后视为完成。

### P1 数据地基

目标：建立可回测的行情事实库。

内容：

- OKX 作为主数据源。
- Binance 作为 BTC/ETH 校验源。
- DuckDB/Parquet 历史行情。
- OKX 历史 K 线下载器。
- 数据质量检查。
- 严禁混合交易所价格作为成交价格。

已完成：

- OKX ticker/candles/instruments 公开行情适配。
- `after/before` 历史分页参数。
- `OKX_CLI_COMMAND` 路径配置。
- 资产宇宙和 canonical symbol 映射。
- DuckDB K 线仓库。
- `DownloadState` 下载状态，按 `venue + inst_type + inst_id + bar` 隔离。
- OKX 历史 K 线下载器 v1，默认小批量下载 confirmed K 线到 `storage/history.duckdb`。

下一小计划：数据质量检查与第一版回测/风控模型。

### P2 指标与特征层

目标：把市场状态转成多策略可复用的 feature/context。

内容：

- 趋势体制：EMA、ADX、ER、CHOP。
- 价格行为：BOS、CHoCH、流动性扫荡、订单块、FVG。
- 量价分析：放量突破、吸收、停止量、缩量回踩、异常量。
- 多周期对齐，避免读取未收盘高周期 K 线。

已完成：

- True Range。
- Kaufman ER。
- Choppiness Index。
- 初步 regime 分类。

### P3 策略轻插件层

目标：让未来新增策略保持低耦合。

内容：

- 定义 `Strategy` 接口和策略注册表。
- 当前主策略作为第一个默认插件：趋势判断 -> 价格行为 -> 量价确认。
- 未来可新增均值回归、突破、事件驱动等策略插件。
- 策略只生成信号和解释，不直接管理账户、不直接下单。

原则：

- 数据层不依赖具体策略。
- 指标层不依赖具体策略。
- 回测层和风控层只消费标准信号。
- 暂不做复杂动态插件扫描，避免过早工程化。

### P4 回测与风控层

目标：验证策略是否在扣除成本后仍有长期优势。

内容：

- 无前瞻偏差撮合。
- 手续费、点差、滑点、资金费率。
- 仓位、止损、止盈、回撤、冷却、熔断。
- 交易日志、权益曲线、三周期排名。
- 输出净利润、最大回撤、胜率、盈亏比、Profit Factor、平均持仓时间、单笔期望、手续费占毛利比例。

默认风控：

- 单笔风险：权益的 `0.5%`。
- 单日最大模拟回撤：`2%`。
- 总体最大模拟回撤：`5%`。
- 达到 1R 后可部分止盈，剩余仓位移动到真实盈亏平衡点。

### P5 Streamlit 本地网页看板

目标：在模拟盘前先有可视化观察面板。

技术路线：Streamlit。

第一版只读本地 DuckDB 和报告文件，不做真实交易控制。

看板内容：

- 回测收益曲线。
- 交易列表。
- 策略信号。
- 风控拒绝原因。
- 周期组对比。
- 数据质量状态。
- 日志和复盘摘要。

### P6 模拟盘 + 解释/复盘/优化

目标：形成持续解释、复盘、优化的闭环。

内容：

- 实时公开行情。
- 虚拟订单。
- 虚拟持仓。
- 虚拟权益曲线。
- 信号解释。
- 亏损复盘。
- 参数优化建议。
- 日报、周报、策略健康报告。

Agent 从本阶段开始持续参与解释、复盘和优化，但不越权下单。

### P7 小资金实盘准备与实盘

目标：在模拟盘稳定后，进入受控真实环境。

内容：

- API 权限隔离。
- 默认关闭真实下单。
- 人工审批。
- 额度限制。
- 熔断和审计日志。
- 实盘阶段继续解释、复盘、优化。

实盘是独立阶段，不和模拟盘混在一起。

### P8 云端 Hermes Agent 部署

目标：在模拟盘和实盘机制稳定后，迁移到长期运行环境。

内容：

- 云端常驻任务。
- 24 小时监控。
- 日报、周报、健康报告。
- 异常提醒。
- 参数建议和复盘归档。

云端 Agent 不改变核心交易规则，只负责任务编排、解释和复盘。

## 数据源策略

- OKX 是主数据源，用于回测成交、模拟盘主价格和后续实盘对齐。
- Binance 是 BTC/ETH 校验源，用于缺口、异常和价格偏差检查。
- XAUT 的 Binance 校验能力必须用 exchangeInfo 动态验证，不能默认可用。
- 不把 OKX 和 Binance K 线混成综合成交价格。
- funding、open interest、宏观、链上、新闻等扩展数据独立存储，只能作为特征或过滤器。

## 当前完成度

截至 2026-05-14：

- P0：Git、`.venv`、依赖、测试体系已建立；文档体系正在精简。
- P1：OKX 适配器、资产宇宙、DuckDB 仓库、OKX 历史下载器 v1 已完成；数据质量检查待做。
- P2：市场体制基础指标已完成一部分。
- P3-P8：尚未实现。

当前测试基线：

```powershell
.\.venv\Scripts\python -B -m unittest discover -s tests -v
```

## 下一小计划

数据质量检查与第一版回测/风控模型：

- 检查 OKX 历史库是否有缺口、重复、未确认 K 线和异常时间顺序。
- 输出每个 `inst_id + bar` 的数据质量摘要。
- 开始定义回测与风控模型：信号、订单、持仓、成本、交易日志。
- 保持模拟和回测为唯一执行路径，不触发真实下单。

## 更新规则

- 阶段状态、下一步计划、风险边界：更新 `项目总规划.md`。
- 策略逻辑、回测指标、风控规则：更新 `策略规格.md`。
- 代理分工、执行规则、模型约定：更新 `代理协作流程.md`。
- 使用方式、目录结构、命令：更新 `README.md`。
