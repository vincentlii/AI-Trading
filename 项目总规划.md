# 项目总规划

本文档是交易系统的主规划文档，记录最终愿景、阶段路线、当前完成度、下一小计划和风险边界。后续每个阶段完成后，优先更新本文档。

## 最终愿景

构建一套可长期运行的量化交易研究与执行辅助系统，用确定性规则完成交易热路径，用 Agent 做慢速解释、复盘、红队审查和优化建议。

核心链路：

```text
MarketRegime -> PriceActionSetup -> VolumePriceConfirmation -> RiskDecision -> SimulatedOrder -> PositionState
```

业务表达：

```text
趋势判断 -> 价格行为 -> 量价确认/否决 -> 风控执行 -> 复盘进化
```

第一阶段不接入真实资金自动下单。系统先完成数据、策略蓝图、回测、看板、模拟盘和解释闭环，再进入小资金实盘准备，最后才部署到云端 Hermes Agent。

## 支持范围

第一版资产：

- `BTC/USDT`
- `ETH/USDT`
- `XAUT/USDT`

扩展预留：

- 纳斯达克指数等 `INDEX` 标的，未来通过独立数据源 adapter 接入。

第一版运行模式：

- 历史行情下载与数据质量检查。
- 多策略插件架构与初始策略插件。
- 策略蓝图 v1 固化与确定性状态机实现。
- 三周期并行回测。
- 本地 Streamlit 网页看板。
- 模拟盘虚拟订单和虚拟持仓。
- 交易日志、解释、复盘、优化建议。

暂不支持：

- 未经人工审批的真实资金自动下单。
- 高杠杆合约实盘。
- 期权、跨所搬砖、复杂套利。
- 云端 24 小时部署前的无人值守实盘。
- LLM/Agent 直接生成买卖指令或绕过风控。

## 阶段路线

### P0 项目地基

目标：让项目可维护、可回滚、可验证。

内容：

- Git 版本控制。
- 本地 `.venv`。
- `requirements.txt`。
- 测试体系。
- D 盘项目目录和存储边界。
- 四份核心文档体系。

状态：进行中，文档体系已精简；后续继续按阶段更新核心文档。

### P1 数据地基

目标：建立可回测的行情事实库。

内容：

- OKX 作为第一版加密场内执行/回测价格源。
- Binance 作为 BTC/ETH 校验源。
- DuckDB/Parquet 历史行情。
- OKX 历史 K 线下载器。
- 数据质量检查。
- 严禁混合交易所价格作为成交价格。

已完成：

- OKX ticker/candles/instruments 公开行情适配。
- `after/before` 历史分页参数。
- `OKX_CLI_COMMAND` 路径配置。
- 资产宇宙和 canonical symbol 映射。
- DuckDB K 线仓库。
- `DownloadState` 下载状态，按 `venue + inst_type + inst_id + bar` 隔离。
- OKX 历史 K 线下载器 v1，默认小批量下载 confirmed K 线到 `storage/history.duckdb`。
- 可扩展标的物骨架：`InstrumentSpec`、`VenueSymbol`，并预留 `INDEX` 资产类别。
- 数据质量检查器 v1，覆盖空数据、缺口、重复、未确认 K 线、OHLC 异常和负成交量。

后续进阶项见“进阶功能回补清单”的 P1。

### P2 指标与特征层

目标：把市场状态转成多策略可复用的 feature/context。

内容：

- 趋势体制：EMA、ADX/DMI、ER、CHOP、TTM Squeeze。
- 价格行为：Swing、BOS、CHoCH、流动性扫荡、Equal High/Low、订单块、FVG、Displacement。
- 量价分析：ToD RVOL、Volume Z-Score、Spread Ratio、放量突破、吸收、停止量、缩量回踩、异常量。
- 多周期对齐，避免读取未收盘高周期 K 线。
- 输出统一 `MarketRegimeContext`、`PriceActionSetup`、`VolumePriceConfirmation`。
- 指标通过轻量注册表暴露元数据，供策略声明依赖。

已完成：

- True Range。
- Kaufman ER。
- Choppiness Index。
- EMA。
- ATR。
- ADX/DMI。
- TTM Squeeze。
- 初步 regime 分类。
- 轻量指标注册表。
- `trend_price_volume_v1` 第一版 `MarketRegimeContext`、`PriceActionSetup`、`VolumePriceConfirmation`。

### P3 策略轻插件层

目标：让未来新增策略保持低耦合。

内容：

- 定义 `Strategy` 接口和策略注册表。
- 当前主策略作为第一个默认插件：`trend_price_volume_v1`。
- 主策略先拆为 `trend_continuation` 与 `liquidity_reversal` 两个确定性剧本。
- PA+VPA 多策略族蓝图：基于研究文档去重，形成可逐步代码化的策略族规格。
- 未来可新增均值回归、突破、事件驱动等策略插件。
- 策略只生成标准信号和只读解释载荷，不直接管理账户、不直接下单。
- 每个策略拥有独立目录，包含自然语言 `strategy.md` 和代码 `strategy.py`。

原则：

- 数据层不依赖具体策略。
- 指标层不依赖具体策略。
- 回测层和风控层只消费标准信号。
- 暂不做复杂动态插件扫描，避免过早工程化。

已完成：

- `trend_price_volume_v1.generate_signals()` 已能基于三周期上下文生成标准 `StrategySignal` 候选。
- 已实现 `trend_continuation` 与 `liquidity_reversal` 两个第一版确定性剧本。
- 策略仍只输出信号、证据和解释载荷，不管理账户、不下单。
- PA+VPA 策略体系 v1 已完成自然语言蓝图，Gemini/GPT 两份 Top 10 研究已去重为 8 个策略族。
- 当前可执行策略 `trend_price_volume_v1` 已明确映射到 PA+VPA 体系中的 01 `liquidity_sweep_reclaim` 与 04 `breakout_pullback_continuation`。

### P4 回测与风控层

目标：验证策略是否在扣除成本后仍有长期优势。

内容：

- 无前瞻偏差撮合。
- 手续费、点差、滑点、资金费率。
- 仓位、结构止损、1R 部分止盈、真实盈亏平衡点、Chandelier Exit、时间止损。
- 组合热度、回撤降权、冷却、熔断。
- 交易日志、权益曲线、三周期排名。
- 输出净利润、最大回撤、胜率、盈亏比、Profit Factor、平均持仓时间、单笔期望、手续费占毛利比例。

默认风控：

- 单笔风险：权益的 `0.5%`。
- 单日最大模拟回撤：`2%`。
- 高水位回撤 `2%/4%/5%` 分别触发降权、只允许高等级信号、硬熔断。
- 达到 1R 后默认平掉 50%，剩余仓位移动到真实盈亏平衡点。

已完成：

- 第一版回测/风控领域模型：`OrderIntent`、`RiskParameters`、`AccountState`、`CostEstimate`、`RiskDecision`、`SimulatedOrder`、`PositionState`、`TradeLogEntry`。
- `RiskEngine` 初版，支持单笔风险、止损距离、名义价值、总杠杆、组合热度、回撤降权、日内亏损限制、流动性反转最低 `1.5R` 目标检查。
- P4.1 信号级回测闭环：`StrategySignal` 转 `OrderIntent`、风控审批、最小无前瞻撮合、交易日志、权益曲线和汇总指标。
- P4.2 完整历史滚动信号扫描器 v1：从仓库读取 confirmed K 线，按 A/B/C profile 滚动构建 `StrategyContext`，调用策略生成信号，接入 P4.1 执行层，并按 `strategy_family`、`setup_type`、资产和周期组输出分层统计。

阶段批次：

- P4.1：信号接入 `RiskEngine` + 最小无前瞻撮合。
- P4.2：完整历史滚动信号扫描器，覆盖多标的、多周期、全历史窗口，并按 `strategy_family`、`setup_type`、资产和周期组输出统计。v1 已完成。
- P4.3：高级出场，包含 1R 减仓、真实盈亏平衡、Chandelier Exit、时间止损。
- P4.4：三周期 A/B/C 批量回测、指标排名、成本淘汰规则。
- P4.5：参数 proposal 队列与回测验证入口。

### P5 Streamlit 本地网页看板

目标：在模拟盘前先有可视化观察面板。

技术路线：Streamlit。

第一版只读本地 DuckDB 和报告文件，不做真实交易控制。

看板内容：

- 回测收益曲线。
- 交易列表。
- 策略信号。
- 风控拒绝原因。
- 周期组对比。
- 数据质量状态。
- 日志和复盘摘要。

### P6 模拟盘 + 解释/复盘/优化

目标：形成持续解释、复盘、优化的闭环。

System 1/System 2 边界：

- System 1 是确定性策略、回测、模拟盘和风控代码，只消费已确认 K 线、固定参数、可测试规则。
- System 2 是异步 Agent 分析层，只读取结构化日志和报告，负责解释、复盘、红队审查、日报/周报和参数建议。
- Agent 输出只进入 proposal 队列，不进入执行路径。
- 参数建议必须经过回测、样本量检查、成本检查和人工确认后，才能成为下一版确定性参数。

内容：

- 实时公开行情。
- 虚拟订单。
- 虚拟持仓。
- 虚拟权益曲线。
- 信号解释。
- 风控拒绝解释。
- 亏损归因。
- 参数优化建议。
- 日报、周报、策略健康报告。

Agent 从本阶段开始持续参与解释、复盘和优化，但不越权下单、不自动改参数。

### P7 小资金实盘准备与实盘

目标：在模拟盘稳定后，进入受控真实环境。

内容：

- API 权限隔离。
- 默认关闭真实下单。
- 人工审批。
- 额度限制。
- 熔断和审计日志。
- 实盘阶段继续解释、复盘、优化。

实盘是独立阶段，不和模拟盘混在一起。

### P8 云端 Hermes Agent 部署

目标：在模拟盘和实盘机制稳定后，迁移到长期运行环境。

内容：

- 云端常驻任务。
- 24 小时监控。
- 日报、周报、健康报告。
- 异常提醒。
- 参数建议和复盘归档。

云端 Agent 不改变核心交易规则，只负责任务编排、解释和复盘。

## 数据源策略

数据源按资产类别分层，不能把所有标的都简化为“单一交易所主源 + 其他交易所校验”。

### Crypto 数据源原则

- BTC/ETH 仍以 OKX 作为第一版主执行/回测价格源。
- Binance 作为 BTC/ETH 校验源，用于缺口、异常和价格偏差检查。
- 不把 OKX 和 Binance K 线混成综合成交价格。
- funding、open interest、链上、新闻等扩展数据独立存储，只能作为特征、过滤器或复盘依据。
- v1 优先用 OKX quote volume 做量价统计，同时保留原始 volume。

### Gold 数据源原则

- XAUT/USDT 是加密场内可交易映射资产或执行价格源，不是全球黄金价格的唯一事实源。
- 黄金主参考源优先评估 COMEX Gold `GC/MGC`，用于高频价格发现和可交易参考。
- XAU/USD 现货黄金用于全球现货参考，后续需评估 LSEG/WMR、券商或专业行情供应商。
- LBMA Gold Price 适合作为 AM/PM 慢速权威基准，不适合分钟级触发。
- XAUT/USDT 出现插针、局部异常波动或盘口偏离时，必须与 GC/MGC、XAU/USD、LBMA/参考源做偏差检查；若参考源未同步，标记为 `venue_liquidity_anomaly`，不得直接解释为黄金趋势破位。

### Nasdaq 数据源原则

- 纳指不使用低质量指数币、指数代币、未授权 CFD 或加密场内映射资产作为主参考源。
- Nasdaq 主参考源优先评估 CME Nasdaq-100 `NQ/MNQ` 期货，用于接近实时、流动性更强的可交易参考。
- Nasdaq-100 Index / `NDX` 适合作为指数基准和成分股事实源，不等同可交易执行价格。
- `QQQ` ETF 适合作为美股 RTH 时段校验源和流动性参考，不替代期货夜盘。
- 纳指信号需要记录 RTH/ETH session、宏观事件和主市场健康状态；场内映射价格明显偏离 NQ/MNQ、NDX、QQQ 时，不得作为趋势破位依据。

### 数据源角色

| 角色 | 用途 | Crypto | Gold | Nasdaq |
| --- | --- | --- | --- | --- |
| 执行/撮合价格源 | 回测、模拟盘、未来实盘对齐 | OKX，后续可扩展交易所 | XAUT/USDT 仅限交易该映射资产时使用 | 后续券商/期货/ETF 可交易源 |
| 主参考价格源 | 趋势、结构和跨市场事实背景 | OKX + Binance 偏差校验 | COMEX GC/MGC、XAU/USD | CME NQ/MNQ、NDX |
| 异常校验源 | 插针、缺口、局部异常识别 | Binance / 其他主流交易所 | GC/MGC、XAU/USD、LBMA | NQ/MNQ、NDX、QQQ |
| 宏观/时段背景源 | 过滤器、解释和复盘 | funding、open interest、链上/新闻 | LBMA、美元、利率、避险事件 | RTH/ETH、宏观日历、成分股/QQQ |

官方依据记录：

- [CME Nasdaq-100 Futures](https://www.cmegroup.com/markets/equities/nasdaq/nasdaq-futures.html)
- [Nasdaq-100 Index Methodology](https://indexes-qc.nasdaqomx.com/docs/Methodology_NDX.pdf)
- [Invesco QQQ](https://www.invesco.com/qqq-etf/en/home.html)
- [CME Gold Futures](https://www.cmegroup.com/markets/metals/precious/gold-futures.html)
- [LBMA Gold Price](https://www.lbma.org.uk/prices-and-data/lbma-gold-price)
- [ICE LBMA Precious Metals](https://www.ice.com/iba/lbma-precious-metals)
- [World Gold Council: Global Gold Market](https://www.gold.org/gold-market-structure/global-gold-market)
- [LSEG WMR Metals](https://www.lseg.com/en/ftse-russell/benchmarks/wmr-fx-benchmarks/wmr-metals)
- [Tether Gold docs](https://docs.alloy.tether.to/alloy-by-tether/alloy-by-tether-vaults/xaumnt)

## 进阶功能回补清单

本清单用于记录已经明确需要做、但当前阶段尚未实现的功能。后续推进时按 P0-P8 主线逐步补齐，避免功能遗漏在对话上下文中。

### P0 待补齐

- 提交节奏固化：每完成一个明确小计划提交一次。
- 环境一致性检查：确认 Python、DuckDB、OKX CLI、Node/前端依赖版本。
- 依赖锁定：在依赖增加后补充可复现安装策略。

### P1 待补齐

- Binance 校验源 adapter。
- XAUT 在 Binance 的可用性动态验证。
- DuckDB 到 Parquet 的导出与归档。
- 数据修复报告：缺口、重复、异常 OHLC、异常成交量的修复建议。
- OKX/Binance 跨交易所价格偏差检查。
- CME `NQ/MNQ` Nasdaq 期货 adapter 研究。
- CME `GC/MGC` Gold 期货 adapter 研究。
- XAU/USD 专业行情源评估：LSEG/WMR、券商或专业行情供应商。
- LBMA Gold Price 授权、可用性和慢速基准接入评估。
- XAUT/USDT 场内流动性、盘口深度和异常插针健康检查。
- 非加密资产跨市场偏差检查器：识别 `venue_liquidity_anomaly`、参考源断连和场内映射资产失真。

### P2 待补齐

- ToD RVOL `20-30` 天分时段历史基线。
- OB/FVG 生命周期：`active`、`mitigated`、`expired`、`invalidated`。
- Equal High/Low 和完整 Swing 层级。
- Volume Z-Score、Spread Ratio、Close Location、Wick Ratio。
- 异常量分类：放量突破、吸收、停止量、缩量回踩、高量低结果。
- AVWAP / Session VWAP，用于扫荡、吸收、HVN/FVG 拒绝和 Nasdaq 适配。
- BTC/ETH 的 taker imbalance、成交笔数等量价增强字段。
- Nasdaq 的 RTH/ETH session、宏观事件标签和真实成交量特征。
- 非加密资产特征上下文：`reference_price_source`、`execution_price_source`、`validation_price_source`、`market_session_context`、`venue_liquidity_anomaly`。
- Gold 的 COMEX/XAUUSD/LBMA 分层参考特征和 XAUT 场内偏差状态。
- Nasdaq 的 NQ/MNQ、NDX、QQQ 分层参考特征和 RTH/ETH 时段状态。

### P3 待补齐

- 多策略插件接入规范，用于未来新增 10 个策略。
- 策略版本化和参数 schema。
- 策略启停配置。
- 多策略共存时的信号优先级和冲突处理规则。
- 将 PA+VPA 8 个自然语言策略族逐步代码化：当前仅 01/04 有早期实现，02/03/05/06/07/08 尚未实现。
- `trend_price_volume_v1` 代码优化：`trend_continuation` 保持硬趋势门控；`liquidity_reversal` 改为软趋势门控和风险降权。
- `StrategySignal.explanation_payload` 增加 `strategy_family`、`trend_gate_role`、`asset_profile`。
- `StrategySignal.explanation_payload` 增加数据源解释字段：`reference_source`、`execution_source`、`validation_source`、`cross_market_deviation`、`market_session`。
- 策略参数按资产差异配置：BTC、ETH、XAUT、Nasdaq 不使用完全相同阈值。

### P4 待补齐

- P4.2 进阶：扫描结果持久化、报告导出、CLI 入口、真实 DuckDB 冒烟样本。
- P4.2 进阶：position-aware 非重叠持仓扫描，避免同一资产同一策略在持仓期重复开仓。
- P4.3 高级出场：1R 减仓、真实盈亏平衡、Chandelier Exit、时间止损。
- P4.4 三周期 A/B/C 批量回测、指标排名、成本淘汰规则。
- P4.5 参数 proposal 队列与回测验证入口。
- 组合持仓、相关性敞口和成本敏感性分析。
- PA+VPA 8 个策略族的分层回测：先验证 01/04，再验证 07，随后验证 02/03/05/06/08。
- 回测结果按执行源价格、主参考源、跨市场偏差状态和 `venue_liquidity_anomaly` 分层统计。
- XAUT/USDT 信号在参考源未同步确认时的拒单、降权或单独分组评估。
- Nasdaq 映射价格或低质量 CFD 明显偏离主参考源时的拒单和异常样本隔离。

### P5 待补齐

- Streamlit 本地网页看板。
- 回测收益曲线、交易列表、信号解释、风控拒绝原因。
- 周期组对比、数据质量状态、日志和复盘摘要。
- 跨市场偏差、场内异常、参考源健康状态和数据源角色展示。

### P6 待补齐

- 模拟盘事件循环。
- 虚拟订单、虚拟持仓和虚拟权益曲线。
- 复盘报告和亏损归因。
- Agent proposal 队列：参数建议只入队，不自动落地。

### P7 待补齐

- 小资金实盘权限隔离。
- 人工审批、额度限制、审计日志。
- 实盘熔断和异常回退流程。
- 实盘阶段持续解释、复盘和优化。

### P8 待补齐

- Hermes Agent 云端运行。
- 日报、周报、策略健康监控。
- 异常提醒和长期复盘归档。
- 云端任务编排，不改变核心交易规则。

## 当前完成度

截至 2026-05-17：

- P0：Git、`.venv`、依赖、测试体系已建立；四份核心文档体系已形成。
- P0：`开发记录.md` 已建立，用于沉淀长期约定、环境限制、阶段状态和验证原则。
- P1：OKX 适配器、资产宇宙、DuckDB 仓库、OKX 历史下载器 v1、数据质量检查器 v1 已完成。
- P1：非加密资产数据源原则已固化；CME、XAU/USD、LBMA、券商/专业行情 adapter 尚未实现。
- P2：市场体制指标、初始策略特征对象和第一版价格行为/量价确认已完成。
- P3：策略接口、策略注册表、初始策略插件 `trend_price_volume_v1`、第一版标准信号生成、PA+VPA 8 策略族自然语言蓝图已完成。
- P3：当前代码只实现 PA+VPA 01/04 的早期子集，02/03/05/06/07/08 和趋势软门控代码优化仍待实现。
- P4：第一版回测/风控领域模型、P4.1 信号级回测闭环、P4.2 完整历史滚动信号扫描器 v1 已完成。
- P5-P8：尚未实现。

当前测试基线：

```powershell
.\.venv\Scripts\python -B -m unittest discover -s tests -v
```

## 下一小计划

进入 P4.3：实现高级出场。

- 在 P4.1/P4.2 已有整仓进出基础上，加入 1R 减仓、真实盈亏平衡、Chandelier Exit 和时间止损。
- 保持无前瞻撮合；同一根 K 线触发多个出场条件时继续使用保守优先级。
- 出场事件必须进入交易日志，供后续 P5 看板和 P6 复盘解释。
- 继续保持模拟和回测为唯一执行路径，不触发真实下单。

## 更新规则

- 阶段状态、下一步计划、风险边界：更新 `项目总规划.md`。
- 策略系统接口、标准信号、插件规则：更新 `策略规格.md`。
- 具体策略逻辑：更新对应 `trading_system/strategies/<strategy>/strategy.md`。
- 代理分工、执行规则、模型约定：更新 `代理协作流程.md`。
- 使用方式、目录结构、命令：更新 `README.md`。
