---
type: progress
status: current
updated: 2026-06-19
tags:
  - status/current
---

# 当前完成度

结论：P0-P5 已形成可验证 v1 地基，Research Pipeline / Full Audit Gate 已成为后续策略研究主路径；LR 历史候选已撤销，Causal Rebuild 在 v3 confirmation geometry 与 fixed-horizon 诊断后正式停止。正式模拟盘和真实交易仍未启用。

## 已完成

- P0：Git、`.venv`、依赖、测试体系、配置化地基、Obsidian 长期知识源。
- P1 v1：OKX 适配器、DuckDB 仓库、历史下载器、数据质量检查。
- P2 v1：市场体制指标、初始策略特征对象、价格行为/量价确认 v1。
- P3 v1：策略接口、注册表、`trend_price_volume_v1`、PA+VPA 8 策略族蓝图。
- P4 v1：风险模型、回测闭环、滚动扫描、高级出场、批量排名、proposal 验证。
- P5 v1：本地 Streamlit 只读看板已升级为中文交易诊断驾驶舱，包含近期 200 window 诊断、P4 完整回测摘要、拒绝细分、风控分析、绩效报告、数据质量和 Proposal 队列。
- P6 gate 能力 v1：`PaperTradingEngine`、paper broker、虚拟 fill / position、退出事件、虚拟权益曲线、JSONL ReviewLog、公开行情事件源、position-aware 回测、无前瞻审计、P6 gate CLI 和 P5 paper 只读展示。
- LR Causal Rebuild：Phase 0-1 因果 anatomy 已完成；Phase 2 Entry Tournament v2 已完成完整 development 审计，但五种入场整体在 base/stress/harsh 成本下均为负期望，未选择 winner。
- LR Multi-Timeframe + VPA causal event research：完整 development 已完成并通过因果与 artifact 审计；后续 v3 证明 confirmation 改善主要来自 chase geometry，固定时间收益缺乏去重与分组稳定性。
- Research Pipeline v1：strategy adapter / registry / CLI、artifact reader / index、research run registry、candidate schema / cache / lineage、full-audit gate、proposal boundary audit、metric recompute、no-lookahead audit、edge / tag / sizing / robustness analytics、legacy wrapper compatibility 和 final regression baseline。

## 未完成

- P1-P5 进阶增强：数据修复、跨交易所偏差、指标特征扩展、PA+VPA 未实现策略族、artifact 持久化和看板 drill-down。
- LR：状态为 `research_stopped`；Restricted Variant B 的 183 笔历史结果仅作追溯，holdout 保持封存。除非出现不同且可预注册的新上游机制，否则不继续 event、entry、exit、sizing 或 threshold 优化。
- P6 验收：当前没有 LR formal research candidate；任何策略进入正式 P6 仍需独立 gate、人工确认和 live path 边界检查。
- P7：小资金实盘权限隔离、人工审批、审计和熔断。
- P8：Hermes Agent 云端运行和长期报告。

## 当前前置缺口

- `trend_price_volume_v1` 策略边界已明确，但仍需在后续回测和诊断中持续验证。
- 多周期 confirmed K 线无前瞻审计、position-aware 一致性能力已接入 `scripts/check_p6_gate.py`。
- BTC/ETH 历史数据覆盖需要达到正式回测要求后再进入模拟盘。
- 后续新策略不得复制 PR11A-PR11H 临时脚本流，应走统一 Research Pipeline、Full Audit Gate、robustness 和 final regression baseline。

## 权威来源

完整状态以本 vault 的 [[项目驾驶舱]]、[[当前阶段-P6]]、[[待补齐清单]] 为准。
