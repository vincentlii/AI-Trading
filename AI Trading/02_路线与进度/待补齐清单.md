---
type: backlog
status: active
updated: 2026-06-04
tags:
  - status/backlog
---

# 待补齐清单

结论：本页是后续计划的唯一长期待执行清单；根目录规划文档只做入口，不再维护另一份正文。

## 使用规则

- `P6 模拟盘前必须补齐` 是进入正式 P6 模拟盘的 gate；LR formal research candidate 不自动进入 live path。
- `P1-P5 进阶增强` 是后续完善 backlog，默认不阻塞 BTC/ETH paper v1，除非单项被提升为 gate。
- 若某阶段先实现 MVP，MVP 外的完整功能、增强项和暂缓能力必须同步写入本页，不能只靠上下文记忆保留。
- 已完成事项必须从本页移出，并记录到 [[里程碑日志]] 或 [[当前完成度]]。
- 新任务优先写入本页，再由阶段页引用，避免散落在多个页面。

## P6 模拟盘前必须补齐

- 默认运行 `scripts/check_p6_gate.py` 必须通过后，才允许进入正式 P6 模拟盘观察。
- LR Restricted Variant B 若进入模拟盘观察，必须保持 `live_trading_enabled=false`，并确认 full-audit、robustness、exposure restriction、final regression baseline 和人工审批均无阻断。
- 新策略进入 formal candidate 前，必须通过 Research Pipeline 的 full-audit、no-lookahead、metric recompute、robustness、exposure restriction 和 regression baseline。

## Research Pipeline 后续复用

- 优先选择一个下一策略验证统一 pipeline 复用性，不要同时扩张多个策略。
- `trend_continuation` 已完成 proposal-only research validation、expansion diagnostics、cross-run baseline reuse 和有限 trend definition variants：500/2000 entry windows 均为 strict setup raw candidates=0，当前判定为 `strategy_definition_refactor_needed`，不进入 formal candidate。
- `trend_continuation` expansion diagnostics 已确认 2000 entry windows / 8000 context windows 的第一卡点为 `trend_gate=7840`，其次为 B profile 的 `breakout_rvol=152`、`pullback_rvol=8`；C profile 全部卡在 trend gate。
- `trend_continuation` trend gate 二级原因集中为 `adx_below_25=7840`；有限 variants 仍未产生 candidate_ready、raw candidates 或 closed trades。
- `trend_continuation` 下一步不应继续轻微调 RVOL 或 trend gate 参数；应把当前宽泛 setup 拆成 `breakout_pullback`、`trend_pullback`、`compression_expansion` 等更明确的 setup。
- `compression_expansion` 已完成 proposal-only 正式研究验证，setup_id / adapter / manifest 统一命名为 `compression_expansion`；Obsidian 语义名 `compression_expansion_breakout` 指向同一 setup，不得另建 artifact 命名分支。
- `compression_expansion` 当前为 `diagnostic_candidate`：candidate anatomy / RiskEngine reject diagnostics 与 semantic repair round 已完成，结构止损可降低 `stop_distance_too_near`，但 base/stress/harsh 收益质量仍未稳定转正，不得 formalize。
- `compression_expansion` semantic repair 集中报告位于 `storage/research_runs/compression_expansion/ce_semantic_repair/c8e463f1961589d37eaa3d8e/compression_expansion_semantic_repair_report.md`。
- `compression_expansion` 决策为 B：保留 diagnostic_candidate，但暂停 CE 局部优化，下一步转向 `breakout_pullback`；不得继续添加 CE 参数网格，不得放宽 RiskEngine、margin、成本、notional cap 或正式出场边界。
- `breakout_pullback` 已完成 proposal-only initial research，run_id=`66591695010676daa1e2434a`；当前判定为 D：进入 backlog，原因是样本不足、base/stress/harsh 收益为负、robustness 不稳定，不能 formalize。
- `breakout_pullback` 集中报告位于 `storage/research_runs/breakout_pullback/initial_research/66591695010676daa1e2434a/breakout_pullback_initial_research_report.md`；后续若重启，必须先修策略定义，不得继续扩大参数网格或放宽 RiskEngine / 成本 / margin / notional cap / portfolio heat / exit boundary。
- `breakout_pullback` shared core rebuild 已完成，集中报告位于 `storage/research_runs/breakout_pullback/core_rebuild_round/3b660ac6d760fa52aed21418/trend_continuation_core_rebuild_report.md`；旧 `true_breakout` 早杀样本问题已修复。
- 当前只允许对 `bp_lifecycle_shallow_momentum_v2` 做一次受限 proposal-only subtype validation；其 12 笔 closed trades 在三档成本下为正且 Full Audit 通过，但全部集中于 profile C / short，不能 formalize。
- Research Pipeline 已持久化 `core_event_rows.jsonl`，event lifecycle rows 不得放大 context-window 漏斗，也不得进入 performance metrics。
- Full Audit Gate 已统一“无亏损样本的 PF 不可定义”为 `None`，避免 `Infinity` 非标准 JSON 和 metric recompute 误报。
- Research Pipeline 已支持 cross-run baseline artifact reuse；后续 harden 应继续扩大可复用 artifact 范围，并让更多策略 adapter 声明自己的二级诊断层。
- 后续候选方向：`trend_pullback`、`stopping_volume_retest`；`breakout_pullback` 需定义层修复后才能重启研究。
- 后续策略不得复制 PR11A-PR11H 临时 stage / PR 脚本流；应使用 strategy adapter、full-research pipeline、full-audit gate、robustness report 和 proposal report。
- 任何 proposal / diagnostic / summary row 不得进入 performance metrics；收益指标只能来自 `row_type=closed_trade`。
- 历史文件清理只能在主测试、Research Pipeline tests、manifest/registry/artifact/docs/Obsidian 引用检查通过后执行，并且必须单独 commit。
- `breakout_pullback` cost-aware refinement with trend_state 已完成，run_id=`20260605T173718Z_61df2053b9f1`；`bp_shallow_cost_aware_admission_v3` harsh 接近打平但仍为 -0.0010，未达到 validation-prep。
- TC family trend_state lineage 已修复：candidate/filter/closed_trade coverage=100%，closed_trade unknown share=0%；后续 trend_state 只能用于 diagnostic split，不能手工筛 regime 来提升绩效。
- 下一轮若继续 TC family，只允许做一次 regime-aware diagnostic round；不得新增策略族、扩大交易数、优化 entry timing、进入 P6、formalize 或放宽任何风险/成本边界。

## P1-P5 进阶增强

### P1 数据进阶

- 数据修复报告。
- 跨交易所价格偏差检查。
- 场内流动性异常识别。
- 参考源断连识别。
- XAUT/USDT 外部黄金参考源一致性检查。
- Nasdaq / INDEX 正式研究数据源接入方案。

### P2 指标与特征进阶

- OB/FVG 生命周期。
- ToD RVOL。
- AVWAP / Session VWAP。
- session high/low、previous day high/low。
- VSA 细分特征库：高量低结果、停止量、吸收、缩量测试、放量突破。
- SMC 结构特征：Swing、BOS、CHoCH、Equal High/Low、流动性池。
- CVD/OI、订单簿、taker imbalance 先作为研究或 wrapper 特征，不进入当前 v1 入场必要条件。

### P3 策略族进阶

- [[07_compression_expansion_breakout]] 已完成 `compression_expansion` proposal-only research validation、candidate anatomy、RiskEngine reject diagnostics 和 semantic repair round；当前为 diagnostic candidate，但暂停局部优化，下一步转向 `breakout_pullback`。
- [[04_breakout_pullback_continuation]] 已完成 proposal-only initial research；当前进入 backlog，下一步不是调参，而是修结构位、pullback/relaunch 搜索窗口、OB-like zone、target source 和 stop anchor 语义。
- 后续实现 [[02_stopping_volume_retest]]、[[03_absorption_box_break]]、[[05_failed_breakout_effort_result]]、[[06_climax_exhaustion_reversal]]、[[08_hvn_fvg_rejection_trap]]。
- 每个新增策略族必须输出标准 `StrategySignal`，不得直接下单或绕过 `RiskEngine`。
- 每个新增策略族必须同步补信号漏斗、拒绝原因和 Near Miss 诊断。

### P4 回测与绩效进阶

- P4 跑完后保存 artifact，P5 直接读取 artifact 摘要和明细。
- 更完整绩效报告。
- QuantStats / empyrical 仅通过报告层 adapter 接入，不替代 `BacktestSummary` 或 ranking 主口径。
- vectorbt 仅保留为离线研究和参数扫描候选，不替代 P4/P6 主链路。
- LLM proposal 队列。
- 外部指标 wrapper 扩展。

### P5 看板与诊断进阶

- P5 读取 P4 artifact，避免每次打开看板重复运行完整回测。
- 数据质量、信号漏斗、风控拒绝和 Near Miss 支持按资产、周期、策略族 drill-down。

### P6 后续增强

- ReviewLog DuckDB adapter。
- paper 模拟盘长周期运行监控和日报。
- P6 gate CLI 的慢速全量模式与快速诊断模式分离。
- P5 paper tab 的图表化权益曲线和交易 drill-down。

## 暂时预留

- XAUT/USDT 深度适配。
- Nasdaq / INDEX 研究数据源。
- Databento / IBKR / CME / OpenBB。
- 宏观事件过滤。

## 重点风险项

- 非加密资产数据源不能沿用 Crypto 简化模式。
- Agent proposal 不得自动变成正式参数。
- 模拟盘不能混入真实交易权限。
- 诊断项不能被误写成入场条件。
- 不能通过放宽 RiskEngine、fee、funding、margin、notional cap、stop、target 或正式出场边界制造收益。
## 2026-06-08 TC family final regime-aware refinement

- `tc-family-final-regime-aware-refinement` 已完成，run_id=`20260607T172428Z_bb3914fa269c`。
- 最佳结果仍是 no-filter v3 baseline：closed=416，base/stress/harsh=0.0612/0.0381/-0.0010，PF=1.4068，WF=4/1。
- regime adaptive exit 与 regime cost gate 都未改善 harsh；cost gate 删除的 MRT 交易事后表现更好，判定 overfilter。
- 当前决策：C. Regime-aware rules overfit or overfiltered; revert to v3 baseline。
- TC family 暂停继续局部 refinement；后续待补齐项改为 read-only LR 互补性分析或 simple support/resistance fixed-RR baseline 对照，不得继续手工筛 regime。

## 2026-06-08 TC family cleanup and preservation

- TC family best diagnostic snapshot 已保存：`storage/research_runs/trend_continuation_family/best_diagnostic_snapshot/tc_family_best_diagnostic_snapshot.md`。
- machine-readable snapshot 已保存：`storage/research_runs/trend_continuation_family/best_diagnostic_snapshot/tc_family_best_diagnostic_snapshot.json`。
- cleanup report 已保存：`storage/research_runs/trend_continuation_family/best_diagnostic_snapshot/tc_family_cleanup_and_preservation_report.md`。
- 最佳可复现 artifact 保留在 `bp_shallow_cost_aware_admission_v3` variant 目录；Full Audit、metric recompute、no-lookahead lineage、regression baseline 均保留。
- 已清理失败/重复 replay variants，释放约 137.46 MB；LR final evidence 未触碰。
- 下一步等待人工提供 simple support/resistance fixed RR baseline 规格；当前不提前实现。
