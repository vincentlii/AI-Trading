---
type: backlog
status: active
updated: 2026-05-22
tags:
  - status/backlog
---

# 待补齐清单

结论：本页是后续计划的唯一长期待执行清单；根目录规划文档只做入口，不再维护另一份正文。

## 立即补齐

- 统一 P6 状态口径：当前是 P6 前置审查/补齐阶段，正式 P6 模拟盘暂缓。
- 修复 `trend_price_volume_v1` 策略边界描述：它只是 BTC/ETH OHLCV confirmed K 线 v1 子集，不是完整 PA+VPA/SMC/VSA 策略。
- 明确当前 v1 已实现条件与未实现增强项。
- 明确 04/01 趋势门控分工：04 可硬门控，01 更适合确认、软过滤或风险降权。
- 固化 P5.2 量价拒绝、风控拒绝、Near Miss、`stop_distance/ATR` 诊断验收。
- 建立 Obsidian 单一权威源规则。
- 将根目录五类长期文档改为薄入口。

## 模拟盘前必须补齐

- P6 模拟执行事件循环：最小 v1 已完成，入口为 `trading_system/simulation/paper.py`，后续接实时公开行情和持久化。
- paper broker / execution adapter：最小 v1 已完成，复用 `SignalOrderAdapter` 与 `RiskEngine`，后续补事件源 adapter。
- order、fill、position、risk rejection、ReviewLog 日志：最小 v1 已完成，后续补日志持久化和看板读取。
- position-aware 回测或至少模拟盘前风险一致性校验：模拟盘最小 v1 已将 open positions 回灌到下一次 RiskEngine 决策；回测 position-aware 扫描仍待补。
- 多周期 confirmed K 线无前瞻审计。
- 手续费、滑点、资金费率、成本后 R 倍数。
- 策略失败案例归因页。
- BTC/ETH 历史数据覆盖补齐到正式回测要求。

## 模拟盘后继续增强

- OB/FVG 生命周期。
- ToD RVOL。
- AVWAP。
- session high/low、previous day high/low。
- 更完整绩效报告。
- LLM proposal 队列。
- 外部指标 wrapper 扩展。

## 暂时预留

- XAUT/USDT 深度适配。
- Nasdaq / INDEX 研究数据源。
- Databento / IBKR / CME / OpenBB。
- 订单簿、CVD/OI、taker imbalance。
- 宏观事件过滤。

## 重点风险项

- 非加密资产数据源不能沿用 Crypto 简化模式。
- Agent proposal 不得自动变成正式参数。
- 模拟盘不能混入真实交易权限。
- 诊断项不能被误写成入场条件。
