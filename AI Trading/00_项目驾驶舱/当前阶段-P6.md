---
type: phase
phase: P6
status: preflight
updated: 2026-05-22
tags:
  - phase/P6
  - status/current
---

# 当前阶段-P6

结论：当前不是正式模拟盘阶段，而是 P6 前置审查/补齐阶段；只有完成策略逻辑清晰度、数据诊断、风控诊断和无前瞻审计后，才进入正式 P6 模拟盘。

## 当前口径

| 项目 | 结论 |
| --- | --- |
| 当前阶段 | P6 前置审查/补齐阶段 |
| 正式 P6 模拟盘 | 暂缓 |
| 优先资产 | BTC/USDT、ETH/USDT |
| 暂缓资产 | XAUT/USDT 深度适配、Nasdaq / INDEX |
| 当前核心策略 | `trend_price_volume_v1` |

## 前置修复重点

- 统一 P6 状态口径，避免把“准备进入 P6”误读为“已进入模拟盘”。
- 澄清 `trend_price_volume_v1` 只是 OHLCV confirmed K 线基础上的 v1 子集，不等于完整 PA+VPA/SMC/VSA 策略。
- 明确当前 v1 只依赖 OHLCV、多周期 K 线、confirmed K、基础趋势/波动/量价指标和 P5/P5.1/P5.2 诊断。
- 明确 OB/FVG 生命周期、ToD RVOL、AVWAP、CVD/OI、订单簿、taker imbalance、宏观事件过滤是后续增强，不是当前入场依赖。
- 固化趋势门控：04 周期趋势可作为硬门控；01 周期趋势更适合作为确认、软过滤或风险降权。
- 分开描述入场逻辑、失效条件、止损逻辑、出场逻辑和拒绝原因，避免把诊断项误写成交易条件。
- 保留 P5.2 的量价拒绝细分、风控拒绝细分、Near Miss、`stop_distance/ATR` 分布作为模拟盘前诊断验收。

## P6 目标

建立公开行情到虚拟订单、虚拟持仓、虚拟权益曲线的模拟盘事件循环，并输出可供复盘 Agent 读取的结构化日志。

## P6 边界

| 包含 | 不包含 |
| --- | --- |
| 公开行情读取 | 真实账户交易 |
| 虚拟订单 | API secret 管理 |
| 虚拟持仓 | 自动实盘下单 |
| 虚拟权益曲线 | Agent 直接改参数 |
| 信号解释 | 绕过 RiskEngine |
| 风控拒绝解释 | 未审批 proposal 生效 |
| 复盘报告 | 实盘权限入口 |

## 建议拆分

1. P6 前置：完成 [[待补齐清单]] 的“立即补齐”和“模拟盘前必须补齐”。
2. 模拟盘事件模型：最小 v1 已完成，`PaperTradingEngine` 接收标准信号和 execution candles，后续接公开行情事件源。
3. 虚拟执行链路：最小 v1 已完成，标准信号 -> `SignalOrderAdapter` -> `RiskEngine` -> paper broker -> 虚拟 fill / position。
4. 结构化日志：最小 v1 已完成，记录 `signal_received`、`adapter_rejected`、`risk_rejected`、`order_created`、`fill_recorded`、`position_opened`。
5. Agent 读取接口：只读日志和报告，生成解释、复盘、红队审查和 proposal。
6. 看板进阶：展示权益曲线、交易列表、风控拒绝原因和复盘摘要。

## 相关页面

- [[待补齐清单]]
- [[P6模拟盘设计入口]]
- [[策略规格]]
- [[trend_price_volume_v1]]
- [[回测系统]]
- [[风控系统]]
- [[复盘中心]]
- [[Agent协作总览]]
- [[Proposal验证流程]]

## 验收标准

- 模拟盘只读公开行情，不需要真实交易权限。
- 虚拟订单和虚拟持仓只来自标准信号与 RiskEngine。
- 多周期输入只使用 confirmed K 线，无前瞻偏差审计通过。
- 结构化日志字段稳定，可被 Agent 和后续 RAG 使用。
- 所有参数建议仍进入 proposal 队列，不自动落地。
