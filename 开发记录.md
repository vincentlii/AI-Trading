# 开发记录

本文档记录长期有效的项目知识，避免上下文丢失和重复犯错。单次任务流水账不写在这里；只有会影响后续执行、验证、架构和协作的事项才记录。

## 当前主线

项目按 `项目总规划.md` 的 P0-P8 推进。当前下一小计划是 P4.4：先只围绕 BTC/ETH 做三周期 A/B/C 批量回测、指标排名和成本淘汰规则。

交易热路径保持确定性：

```text
MarketRegime -> PriceActionSetup -> VolumePriceConfirmation -> RiskDecision -> SimulatedOrder -> PositionState
```

LLM/Agent 只做慢速研究、解释、复盘、红队审查和参数建议，不进入买卖决策热路径，不绕过风控。

## 已确认约定

- 每完成一个明确小计划，提交一次 Git commit。
- 目前在 `master` 分支开发，除非用户明确要求，不使用 worktree。
- `交易系统知识/` 是资料目录，默认不纳入代码提交。
- 项目独有依赖、数据、缓存、测试数据库优先放在 `D:\交易系统` 内。
- `.venv` 外的 `__pycache__` / `.pyc` 在完成任务前清理。
- 真实交易接口默认关闭；不在聊天或仓库中保存 API secret、secret key、passphrase。
- 不混合不同交易所 K 线生成综合成交价格；BTC/ETH 以 OKX 作为第一版主执行/回测源，Binance 先作为校验源预留。
- BTC/ETH 可继续使用加密交易所主源 + 校验源结构；黄金和纳指必须使用传统金融主参考源 + 场内执行源 + 偏差校验机制。`XAUT/USDT` 只是加密场内映射/执行源，不是全球黄金唯一事实源。
- 正式配置进入 Git；Agent 只能在 `configs/proposals/` 生成配置修改建议，不能自动修改正式配置、代码或真实交易权限。

## 当前环境规则

- 本项目后续不使用 `rtk`。直接使用 PowerShell、Git、Python 和项目内 `.venv` 执行命令。
- Windows PowerShell 是当前 shell。
- 当前测试基线：

```powershell
.\.venv\Scripts\python -B -m unittest discover -s tests -v
.\.venv\Scripts\python -B -m compileall trading_system tests
```

## 阶段状态摘要

- P0：Git、`.venv`、依赖、测试体系、四份核心文档已建立；本文件补齐开发记录。
- P0/P4 前置：配置化地基 v1 已完成；P4.4 使用 `configs/presets/btc_eth_p4_4.toml` 作为 BTC/ETH 批量回测入口配置。
- P1：OKX 公开行情、DuckDB 仓库、历史下载器 v1、数据质量检查器 v1 已完成。
- P1：非加密资产数据源原则已固化；CME、XAU/USD、LBMA、券商/专业行情 adapter 尚未实现。
- P2：市场体制指标、初始策略特征对象、第一版价格行为/量价确认已完成。
- P3：策略接口、注册表、`trend_price_volume_v1` 第一版信号生成已完成。
- P4：风险模型、P4.1 信号级回测闭环、P4.2 完整历史滚动扫描器 v1、P4.3 高级出场模型 v1 已完成；P4.4 BTC/ETH 批量回测排名待实现。
- P5-P8：尚未实现。

## 验证原则

- 新生产代码必须有测试。
- 核心流程、共享模块、数据流、风控和回测改动必须跑全量测试和 compileall。
- 文档类小任务至少跑 `git diff --check`。
- 不能用“应该通过”替代验证结果；最终回复必须说明实际运行命令和结果。

## 后续优先级

1. P4.4 BTC/ETH 三周期 A/B/C 批量回测、指标排名、成本淘汰规则。
2. P4.5 参数 proposal 队列与回测验证入口。
3. P5 Streamlit 本地网页看板。
