# 策略规格

本文档记录交易系统的策略系统总规范。具体策略的自然语言说明和代码实现放在各自策略目录中，避免未来多策略并存时让单一文档变得臃肿。

当前初始策略：

- 说明文档：`trading_system/strategies/trend_price_volume_v1/strategy.md`
- 代码入口：`trading_system/strategies/trend_price_volume_v1/strategy.py`

当前 PA+VPA 策略体系：

- 总览：`trading_system/strategies/pa_vpa_v1/README.md`
- 去重映射：`trading_system/strategies/pa_vpa_v1/strategy_map.md`
- 自然语言策略规格：`trading_system/strategies/pa_vpa_v1/specs/`

`pa_vpa_v1` 目前是自然语言蓝图，不自动注册为代码插件。当前可执行插件 `trend_price_volume_v1` 只覆盖其中 01 `liquidity_sweep_reclaim` 和 04 `breakout_pullback_continuation` 的第一版子集。

## 策略插件架构

所有策略都作为插件接入，核心原则是低耦合：

- 数据层只提供行情、标的物、交易所映射和质量状态。
- 指标层只提供可复用指标和特征，不绑定某个策略。
- 策略层只生成标准信号和解释载荷，不管理账户、不下单。
- 风控层只消费标准信号，统一做仓位、止损、止盈、冷却和熔断。
- 回测层和模拟盘只面对标准信号、风控决策和模拟订单。

策略目录约定：

```text
trading_system/strategies/
├── base.py
├── registry.py
└── <strategy_name>_<version>/
    ├── strategy.py
    └── strategy.md
```

后续新增策略时，应新增独立目录，不把策略细节写回本总规范。

自然语言策略族目录约定：

```text
trading_system/strategies/
└── <strategy_system>_<version>/
    ├── README.md
    ├── strategy_map.md
    └── specs/
        └── <NN_strategy_family>.md
```

自然语言策略族用于沉淀研究、去重、适配差异和实现边界；只有包含 `strategy.py` 且被注册表加载的目录才是可执行策略插件。

## 配置边界

配置文件只管理经常实验、对比和复盘的参数，不改变交易热路径：

- 适合配置：启用资产、venue、策略启停、setup 启停、风险参数、成本假设、回测执行参数、周期排名规则。
- 必须留在代码：策略状态机、指标计算、风控执行、成本计算公式、无前瞻撮合顺序和真实交易权限边界。
- 正式配置必须进入 Git；Agent 只能在 `configs/proposals/` 生成参数修改建议，不能直接改正式配置或让 proposal 自动生效。
- 每次回测应记录 `config_version` 和 `config_fingerprint`，保证参数可审计、可复现、可回滚。

## PA+VPA 策略族 v1

三份研究文档整合后，当前保留 8 个独立策略族：

| ID | 策略族 | 定位 | 趋势判断角色 |
| --- | --- | --- | --- |
| 01 | `liquidity_sweep_reclaim` | 扫损、假突破、快速收回 | 软过滤 / 反转豁免 |
| 02 | `stopping_volume_retest` | 停止量后缩量二次测试 | 软过滤 / 局部结构确认 |
| 03 | `absorption_box_break` | 高量窄幅吸收后选择方向 | 辅助过滤 |
| 04 | `breakout_pullback_continuation` | 放量突破、缩量回踩、顺势续攻 | 必要条件 / 硬门控 |
| 05 | `failed_breakout_effort_result` | 努力结果背离后的失败突破 | 软过滤 |
| 06 | `climax_exhaustion_reversal` | 高潮量、二次推进衰竭、极值反转 | 风险调节 |
| 07 | `compression_expansion_breakout` | 缩量压缩后的真实扩张 | 必要条件 / 硬门控 |
| 08 | `hvn_fvg_rejection_trap` | HVN/FVG/价值区接受失败 | 环境选择器 |

趋势门控规则：

- 延续类和压缩突破类：趋势判断是必要条件，必须作为硬门控。
- 扫荡、反转、衰竭、失败突破类：趋势判断不能作为统一硬否决；逆势允许，但提高量价阈值、降低仓位、要求更清晰结构确认。
- 吸收和价值区拒绝类：趋势判断主要用于市场环境选择，不能重复证明价格行为和量价已经证明的事情。
- 若趋势判断只重复 PA/VPA 条件，不作为额外门槛；只有提供市场状态、趋势强度、震荡程度、波动阶段或方向背景时才保留。

## 策略接口

策略插件至少提供：

- `StrategyMetadata`
- `StrategyContext`
- `StrategySignal`
- `generate_signals(context)`
- `build_explanation_payload(signal, context)`

`StrategyMetadata` 至少记录：

- `name`
- `version`
- `description`
- `required_timeframe_profile_keys`
- `required_indicators`
- `documentation_path`
- `supported_symbols`
- `setup_types`

`generate_signals()` 在完整逻辑实现前可以返回空 tuple，但必须保持接口稳定。

## 标准信号

标准信号至少包含：

- `strategy_name`
- `strategy_version`
- `setup_type`
- `symbol`
- `venue`
- `timeframe_group`
- `direction`
- `entry_zone`
- `invalidation_level`
- `target_hint`
- `trend_evidence`
- `price_action_evidence`
- `volume_price_evidence`
- `risk_profile`
- `explanation_payload`

策略不得输出真实订单。真实或模拟订单只能由回测/模拟盘执行层在风控通过后生成。

后续 PA+VPA 策略信号建议在 `explanation_payload` 中增加：

- `strategy_family`
- `trend_gate_role`
- `asset_profile`
- `reference_source`
- `execution_source`
- `validation_source`
- `cross_market_deviation`
- `market_session`

这些字段先作为解释载荷约定，不要求立即修改 `StrategySignal` dataclass。

## 三周期回测分层

所有策略可以按需声明所需周期组。当前默认周期组为：

| 组别 | 入场周期 | 结构周期 | 趋势周期 | 用途 | 默认排序 |
| --- | --- | --- | --- | --- | --- |
| B 标准波段 | 15m | 1h | 4h | BTC/ETH/XAUT 主回测组，兼顾信号质量和交易频率 | 1 |
| C 慢趋势 | 1h | 4h | 1d | 低频趋势捕捉，尤其适合 XAUT 和大级别 BTC/ETH | 2 |
| A 日内快节奏 | 5m | 15m | 1h | BTC/ETH 高流动性时段的扫荡、OB/FVG 回踩 | 3 |

不使用 `1m` 作为主策略周期，避免手续费、滑点、假突破和噪音吞噬毛利。

## 指标与特征

指标层通过轻量注册表暴露可复用指标元数据。策略只能声明依赖指标，不能把通用指标私有化在单个策略里。

当前基础指标：

- `regime.true_range`
- `regime.kaufman_efficiency_ratio`
- `regime.choppiness_index`
- `regime.classify_regime`

后续会继续加入趋势体制、价格行为、量价确认、风险成本等指标。指标注册表只负责元数据和发现，不替代具体指标实现。

## 标的物与数据源

标的物使用可扩展的 `InstrumentSpec` / `VenueSymbol` 表达：

- `InstrumentSpec` 记录 canonical symbol、资产类别、报价资产、主数据源、交易所符号和是否默认启用。
- `VenueSymbol` 记录某个 venue 下的真实 symbol。
- 默认下载清单仍保持 `BTC-USDT`、`ETH-USDT`、`XAUT-USDT`。
- `INDEX` 资产类别已预留，用于未来接入纳斯达克指数等非 OKX 资产。

数据源约束：

- 数据源按资产类别定义主源、执行源和校验源，不再把“OKX 主源”机械套到所有资产。
- Crypto：OKX 是第一版主执行/回测价格源；Binance 只作为 BTC/ETH 校验源，不自动进入主回测成交价格。
- Gold：COMEX `GC/MGC`、XAU/USD、LBMA Gold Price 构成参考体系；`XAUT/USDT` 只作为加密场内可交易映射资产或执行价格源，不是全球黄金唯一事实源。
- Nasdaq：优先用 CME `NQ/MNQ`、Nasdaq-100 Index / `NDX`、`QQQ` 构成参考体系；禁止低质量指数币、指数代币或未授权 CFD 作为主参考源。
- 不混合不同交易所或不同市场 K 线生成“综合成交价格”。
- 纳斯达克指数需要未来独立 adapter，不进入 OKX 默认历史下载清单。
- XAUT/USDT 若与 GC/MGC、XAU/USD、LBMA/参考源显著偏离，应进入 `venue_liquidity_anomaly`，策略不得直接把场内插针解释为全球黄金趋势破位。
- 非加密资产信号解释载荷后续必须能区分 `reference_source`、`execution_source`、`validation_source`、`cross_market_deviation` 和 `market_session`。

## 风控边界

风控层统一执行账户风险、仓位裁剪、止损止盈和熔断。策略可以建议风险配置，但不能绕过风控。

当前系统级默认：

- 单笔风险：`0.5% equity`。
- 单日最大模拟回撤：`2%`。
- 总体硬熔断：`5%`。
- 达到 `1R` 后默认部分止盈，剩余仓位移动到真实盈亏平衡点。
- 成本必须计入手续费、点差、滑点和资金费率。
- 所有真实交易接口默认关闭。

具体策略的出场映射写入各自 `strategy.md`。

回测/风控模型入口：

- `trading_system/backtest/risk.py`
- `trading_system/backtest/execution.py`

当前模型覆盖 `OrderIntent`、`RiskParameters`、`AccountState`、`CostEstimate`、`RiskDecision`、`SimulatedOrder`、`PositionState` 和 `TradeLogEntry`。执行层在 `BacktestExecutionConfig(enable_advanced_exits=True)` 时启用 P4.3 高级出场，并把 `partial_take_profit`、`breakeven_stop`、`chandelier_exit`、`time_exit` 等事件写入 `exit_events`。

## 回测输出指标

每个周期组必须输出：

- `net_profit`
- `max_drawdown`
- `win_rate`
- `profit_loss_ratio`
- `profit_factor`
- `average_holding_time`
- `expectancy_per_trade`
- `fee_to_gross_profit_ratio`

周期组判定：

- A 组若 `fee_to_gross_profit_ratio > 25%`，直接标记为 `rejected`。
- C 组若交易次数低于最小样本数，默认 `30`，标记为 `supporting_only`。
- B 组默认是主策略候选；最终排序仍结合净利润、回撤、Profit Factor 和单笔期望。

## 结构化复盘

复盘输出必须结构化，避免自由散文式幻觉。建议字段：

- `review_id`
- `timestamp`
- `asset`
- `venue`
- `timeframe_group`
- `strategy_name`
- `strategy_version`
- `parameter_version`
- `market_regime`
- `setup_type`
- `signal_trace`
- `risk_decision`
- `execution_simulation`
- `outcome`
- `attribution`
- `agent_assessment`
- `parameter_suggestions`
- `memory_update`

`parameter_suggestions.auto_apply` 必须为 `false`。所有建议必须绑定证据、数据窗口、置信度和所需回测。

## 代码入口

策略系统：

- `trading_system/strategies/base.py`
- `trading_system/strategies/registry.py`
- `trading_system/strategies/trend_price_volume_v1/strategy.py`

配置：

- `trading_system/config/loader.py`
- `configs/presets/btc_eth_p4_4.toml`

标的物：

- `trading_system/data/universe.py`

数据质量：

- `trading_system/data/quality.py`

指标：

- `trading_system/indicators/registry.py`
- `trading_system/indicators/regime.py`
