# 策略规格

本文档记录交易系统的策略规格。它只描述策略逻辑、回测分层、指标输出和风控规则，不记录项目进度。

## 策略体系

系统采用轻插件架构。当前默认策略是：

```text
趋势判断 -> 价格行为 -> 量价确认 -> 风控执行
```

未来可以新增其他策略，例如均值回归、突破、事件驱动、跨周期过滤策略。新增策略应作为独立插件实现，不应和数据层、回测层、风控层强耦合。

## 策略接口概念

后续策略插件至少应具备这些概念：

- `name`
- `required_timeframes`
- `generate_signals(context)`
- `explain_signal(signal, context)`
- `risk_profile`

策略插件只负责生成标准信号和解释，不直接管理账户、不直接下单。

## 三周期回测分层

所有周期组使用同一套核心逻辑，只缩放时间框架。

| 组别 | 入场周期 | 结构周期 | 趋势周期 | 用途 | 默认排序 |
| --- | --- | --- | --- | --- | --- |
| B 标准波段 | 15m | 1h | 4h | BTC/ETH/XAUT 主回测组，兼顾信号质量和交易频率 | 1 |
| C 慢趋势 | 1h | 4h | 1d | 低频趋势捕捉，尤其适合 XAUT 和大级别 BTC/ETH | 2 |
| A 日内快节奏 | 5m | 15m | 1h | BTC/ETH 高流动性时段的扫荡、OB/FVG 回踩 | 3 |

不使用 `1m` 作为主策略周期，避免手续费、滑点、假突破和噪音吞噬毛利。

## 默认主策略

### 趋势判断

趋势周期只负责判断市场体制：

- EMA50/200。
- ADX。
- Kaufman ER。
- CHOP。
- BOS 方向。

市场状态至少应区分：

- 高信噪趋势。
- 随机混沌。
- 均值回归/过渡状态。

### 价格行为

结构周期负责标记结构位置：

- 前高前低。
- BOS。
- CHoCH。
- 流动性池。
- 扫荡区。
- 订单块。
- FVG。

### 量价确认

入场周期负责最终触发：

- Pin Bar。
- 吞没。
- CHoCH。
- 停止量。
- 放量突破。
- 吸收。
- 缩量回踩。

量价确认用于过滤假突破和弱回踩，不单独作为交易理由。

## 两类核心策略

### 趋势延续

当高周期确认趋势，低周期出现真实结构突破，并且量价关系支持“高努力高结果”时，系统寻找回踩订单块或 FVG 的入场机会。

出场方式以动态追踪为主，可使用 ATR 吊灯止损、结构止损或分批止盈。

### 流动性反转

当价格扫过明显流动性池后无法延续，并出现吸收、停止量、CHoCH 等反转证据时，系统尝试捕捉短线反转。

止损使用结构止损，目标可参考对侧流动性池。

## 回测输出指标

每个周期组必须输出：

- `net_profit`
- `max_drawdown`
- `win_rate`
- `profit_loss_ratio`
- `profit_factor`
- `average_holding_time`
- `expectancy_per_trade`
- `fee_to_gross_profit_ratio`

## 周期组判定规则

- A 组若 `fee_to_gross_profit_ratio > 25%`，直接标记为 `rejected`。
- C 组若交易次数低于最小样本数，默认 `30`，标记为 `supporting_only`。
- B 组默认是主策略候选；最终排序仍结合净利润、回撤、Profit Factor 和单笔期望。

## 风控规则

系统默认“先活着，再赚钱”。

第一版默认参数：

- 单笔风险：权益的 `0.5%`。
- 单日最大模拟回撤：`2%`。
- 总体最大模拟回撤：`5%`。
- 达到 1R 后可部分止盈，并将剩余仓位止损移动到真实盈亏平衡点。
- 出现异常放量、坏数据、闪崩、疑似拉盘出货时进入冷却期。
- 所有真实交易接口默认关闭。

## 数据约束

- 回测成交价格必须绑定具体 `venue`。
- OKX 是第一版主数据源。
- Binance 只作为校验源，不能自动进入主回测成交价格。
- 只使用已确认 K 线进入回测。
- OKX K 线 `ts` 视为开盘时间，信号最早只能在该 K 线收盘后使用。
- 多周期对齐时，入场周期不能读取尚未收盘的结构周期或趋势周期。

## 代码入口

三周期分层代码：

- `trading_system/timeframe_profiles.py`

对应测试：

- `tests/test_timeframe_profiles.py`
