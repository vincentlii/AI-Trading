# 交易系统产品说明文档 v0.1

## 1. 产品定位

“交易系统”是一套面向加密资产的量化交易研究与模拟执行系统，第一版聚焦 `BTC`、`ETH`、`XAUT` 三类高流动性资产。

系统不以主观喊单为核心，而是把交易判断拆成可计算、可回测、可解释的机器流程：

趋势判断 -> 价格行为 -> 量价确认 -> 风控执行 -> 复盘进化

第一阶段仅支持回测与模拟盘，不接入真实资金下单。

## 2. 核心目标

构建一套 24 小时可运行的自动化交易框架，用来验证一条完整交易逻辑主链路：

1. 判断当前市场是趋势、震荡、压缩、混沌，避免策略错配。
2. 用价格行为识别结构位置，例如 BOS、CHoCH、流动性扫荡、订单块、FVG。
3. 用量价关系确认真假突破、吸收、派发、缩量回踩、停止行为。
4. 用严格风控管理仓位、止损、止盈、回撤、异常行情。
5. 用 LLM/Hermes Agent 做慢速分析、复盘和参数建议，但不直接越权下单。

## 3. 第一版支持范围

交易标的：

- `BTC/USDT`
- `ETH/USDT`
- `XAUT/USDT`

运行模式：

- 历史行情回测
- 模拟盘实时运行
- 交易日志与复盘报告
- Hermes Agent 云端 24 小时部署准备

暂不支持：

- 真实资金自动下单
- 高杠杆合约实盘
- 期权、套利、跨所搬砖
- 可视化前端仪表盘
- 微信/Telegram/邮件告警

## 4. 策略逻辑

系统采用两类核心策略。

回测周期分为三组并行验证，所有组别使用同一套核心逻辑，只缩放时间框架：

| 组别 | 入场周期 | 结构周期 | 趋势周期 | 用途 |
| --- | --- | --- | --- | --- |
| A 日内快节奏 | 5m | 15m | 1h | BTC/ETH 高流动性时段，捕捉扫荡、OB/FVG 回踩 |
| B 标准波段 | 15m | 1h | 4h | BTC/ETH/XAUT 主力回测组，默认主策略候选 |
| C 慢趋势 | 1h | 4h | 1d | XAUT、BTC/ETH 大趋势与低频持仓 |

趋势周期只负责市场体制判断；结构周期负责标记 OB、FVG、流动性池、前高前低和扫荡区；入场周期负责最终触发。第一版不使用 `1m` 作为主策略周期。

趋势延续策略：

当高周期确认趋势，低周期出现真实结构突破，并且量价关系支持“高努力高结果”时，系统寻找回踩订单块或 FVG 的入场机会。该策略不设置固定止盈，主要依赖 ATR 吊灯止损与动态追踪止损。

流动性反转策略：

当价格扫过明显流动性池后无法延续，并出现吸收、停止量、CHoCH 等反转证据时，系统尝试捕捉短线反转。该策略使用结构止损和对侧流动性池作为固定目标。

## 5. 风控原则

系统默认“先活着，再赚钱”。

第一版默认参数：

- 单笔风险：账户权益的 `0.5%`
- 单日最大模拟回撤：`2%`
- 总体最大模拟回撤：`5%`
- 达到 1R 后可部分止盈，并将剩余仓位止损移动到真实盈亏平衡点
- 出现异常放量、坏数据、闪崩、疑似拉盘出货时进入冷却期
- 所有真实交易接口默认关闭

## 6. AI Agent 角色

LLM/Hermes Agent 不直接做交易执行，而是作为“研究员 + 风控复盘员”。

它负责：

- 总结市场状态
- 解释系统为什么产生或拒绝信号
- 复盘亏损交易是否违反规则
- 给出参数优化建议
- 生成日报、周报、策略健康报告

真正的交易热路径由确定性规则执行，避免模型幻觉、延迟和情绪化判断。

## 7. 技术形态

第一版建议使用 Python 实现。

核心组件：

- 行情接入层：CCXT，优先 OKX/Binance
- 数据存储：DuckDB + Parquet
- 指标引擎：趋势、价格行为、量价、异常过滤
- 策略引擎：趋势延续、流动性反转
- 风控引擎：仓位、止损、回撤、冷却、熔断
- 回测引擎：无前瞻偏差撮合
- 模拟盘引擎：实时行情、虚拟订单、虚拟持仓
- Agent 层：复盘、解释、报告、参数建议
- 部署层：Docker + Hermes Agent 常驻运行

## 8. 成功标准

第一版完成后，应满足：

- 能拉取 BTC、ETH、XAUT 的历史 K 线。
- 能对三类资产完成回测。
- 能输出每笔交易的入场、出场、止损、止盈、风控理由。
- 能启动模拟盘并持续运行。
- 系统不会发出真实下单。
- 每日能生成一份交易与市场复盘报告。
- 具备后续迁移到 Hermes Agent 云端 24 小时运行的结构。

## 9. 风险声明

本系统第一版是量化研究与模拟交易工具，不构成投资建议。所有策略必须经过长期回测、模拟盘验证、压力测试和人工审核后，才可考虑进入小资金实盘阶段。
