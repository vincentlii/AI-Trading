# 代理协作流程

本项目采用“策划者 + 代理”的方式推进。本文档只记录协作规则，不记录具体当前任务；阶段状态和下一小计划统一写入 `项目总规划.md`。

每次开始非琐碎任务前，先读取 `开发记录.md`，确认长期约定、环境限制、当前阶段状态和验证命令。

## 模型约定

后续默认使用：

- 策划者主线程：`gpt-5.5`，超高推理强度。
- 执行代理：`gpt-5.5`，高推理强度。

只有当任务可以拆成互不冲突的写入范围，或需要独立研究报告时，才派发代理。普通收束、复盘、清理和小型文档更新由策划者直接完成。

## 项目边界

项目根目录为 `D:\交易系统`。

项目独有内容优先放在该目录内：

- Python 虚拟环境：`D:\交易系统\.venv`
- 历史行情：`D:\交易系统\storage`
- DuckDB/Parquet：`D:\交易系统\storage`
- 回测结果：`D:\交易系统\backtest_results`
- 报告输出：`D:\交易系统\reports`

非必要不向 C 盘写入大文件。全局 CLI 工具可以使用系统默认位置，但项目数据、模型缓存、回测缓存和下载数据不得依赖 C 盘空间。

## 角色分工

### 策划者

策划者由主线程承担，负责：

- 维护项目主线：趋势判断 -> 价格行为 -> 量价确认/否决 -> 风控执行 -> 复盘进化。
- 把大任务拆成互不冲突的小任务。
- 给代理写清楚目标、范围、禁止事项、验收标准和返回格式。
- 审核代理报告与代码变更。
- 整合代码与文档。
- 运行完整测试。
- 更新 `项目总规划.md`。

### 研究代理

研究代理只读项目和工具，不改文件。

适合任务：

- 调研 OKX/Binance 能力。
- 设计行情下载、存储、回测、风控、报告架构。
- 分析前瞻偏差、过拟合、成本模型、样本量问题。
- 审查策略蓝图、量价规则、风控规则和 Agent 边界。
- 输出可交给执行代理实现的具体任务。

### 执行代理

执行代理负责小范围代码落地。

要求：

- 必须有明确写入范围。
- 必须知道自己不是唯一在代码库工作的代理。
- 不得回退或覆盖其他代理/用户的改动。
- 必须使用 TDD：先写测试、确认失败、再实现。
- 必须运行指定测试和完整测试。
- 必须返回修改文件、测试结果、实现说明和遗留风险。

### 复盘代理

复盘代理从模拟盘阶段开始参与，只读结构化日志、回测报告和模拟交易记录。

适合任务：

- 信号解释。
- 风控拒绝原因解释。
- 亏损归因。
- 日报、周报和策略健康报告。
- 红队审查前瞻偏差、过拟合、样本不足、成本吞噬和异常数据。
- 生成参数建议 proposal。

复盘代理不得自动改参数、不得生成真实订单、不得绕过风控。

## Agent 权限矩阵

| 角色 | 读取项目 | 修改文档 | 修改代码 | 运行测试 | 读取结构化交易日志 | 生成参数建议 | 自动应用参数 | 真实下单 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 策划者主线程 | 是 | 是 | 是 | 是 | 是 | 是 | 否，需人工确认 | 否 |
| 研究代理 | 是 | 否 | 否 | 只读/非写入检查 | 可读脱敏摘要 | 是 | 否 | 否 |
| 执行代理 | 是 | 仅限任务范围 | 仅限任务范围 | 是 | 仅限任务需要 | 否 | 否 | 否 |
| 复盘代理 | 是 | 否 | 否 | 否 | 是 | 是 | 否 | 否 |
| 云端 Hermes Agent | 是 | 仅按审批流程 | 否，除非单独授权 | 可运行只读检查 | 是 | 是 | 否 | 否 |

所有参数建议必须进入 proposal 队列，经过回测、样本量检查、成本检查和人工确认后，才能成为下一版确定性参数。

## 快慢系统边界

- System 1：确定性策略、回测、模拟盘和风控代码。只消费已确认 K 线、固定参数、可测试规则，输出标准信号、风控通过/拒绝、虚拟订单、虚拟持仓和审计日志。
- System 2：异步 Agent 分析层。只读取结构化日志与报告，负责解释、复盘、红队审查、日报/周报和参数建议。

LLM/Agent 不得直接决定买卖方向、仓位大小、止损止盈、真实订单或风控覆盖。

## 任务派发格式

每个代理任务必须包含：

- 目标。
- 写入范围或只读范围。
- 必须阅读的上下文文件。
- 禁止事项。
- 验收标准。
- 返回格式。

执行代理返回格式：

```text
STATUS: DONE / DONE_WITH_CONCERNS / BLOCKED
Files changed:
Tests run and results:
Implementation notes:
Risks/concerns:
```

研究代理返回格式：

```text
STATUS: DONE / BLOCKED
Files inspected:
Commands/tools used:
Deep report:
Concrete next tasks:
```

复盘代理返回格式：

```text
STATUS: DONE / BLOCKED
Reports/logs inspected:
Findings:
Risk flags:
Parameter proposals:
Required verification:
```

## 审核流程

每轮代理完成后，策划者按以下顺序处理：

1. 阅读每个代理的 `STATUS`。
2. 若 `BLOCKED`，判断是缺上下文、任务过大、环境问题还是方案错误。
3. 对执行代理的变更运行对应测试和完整测试。
4. 检查是否越界修改文件。
5. 把研究报告转化为下一轮可执行任务。
6. 更新必要文档，优先更新 `项目总规划.md`。
7. 清理 `.venv` 外的 `__pycache__` / `.pyc`。
8. 需要时提交 Git。

## 强制规则

- 所有真实交易接口默认关闭。
- 不在聊天中收集 API Secret、secret key 或 passphrase。
- 默认只用公开行情，账户相关操作单独隔离。
- 不使用 `1m` 作为主策略周期。
- 新生产代码必须有测试。
- 不混合不同交易所 K 线生成“综合成交价格”。
- LLM/Agent 不得自动改参数、不得绕过风控、不得生成真实订单。
- 参数建议必须经回测、成本检查和人工确认后才能落地。
- 每一轮结束都要能回答：新增了什么、验证了什么、下一步做什么。
- 非琐碎任务完成前，检查 `开发记录.md` 是否仍与项目状态一致；如产生长期有效信息，必须更新。
