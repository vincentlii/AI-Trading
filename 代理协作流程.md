# 代理协作流程

结论：Agent 角色、权限、任务模板和协作规则以 `AI Trading/07_Agent协作/` 为准；本文件只保留入口。

## 权威目录

- Agent 协作：`AI Trading/07_Agent协作/`
- 风险边界：`AI Trading/00_项目驾驶舱/`
- 开发约定与验证命令：`AI Trading/01_长期记忆/`
- 长期决策：`AI Trading/06_决策记录/`

## 使用规则

- Agent 不生成真实订单。
- Agent 不自动改正式配置。
- Agent 不绕过 `RiskEngine`。
- Agent 参数建议必须进入 proposal。
- 执行代理必须知道自己不是唯一在代码库工作的代理。

## 更新规则

只有入口目录、恢复路径或说明规则变化时才更新本文件。长期 Agent 规则只写入 Obsidian 权威目录。
