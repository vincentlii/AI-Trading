# 代理协作流程

本项目采用“策划者 + 代理”的方式推进。本文档只记录协作规则，不记录具体当前任务；阶段状态和下一小计划统一写入 `项目总规划.md`。

## 模型约定

后续默认使用：

- 策划者主线程：`gpt-5.5`，超高推理强度。
- 执行代理：`gpt-5.5`，高推理强度。

只有当任务可以拆成互不冲突的写入范围，或需要独立研究报告时，才派发代理。普通收束、复盘、清理和小型文档更新由策划者直接完成。

## 项目边界

项目根目录为 `D:\交易系统`。

项目独有内容优先放在该目录内：

- Python 虚拟环境：`D:\交易系统\.venv`
- 历史行情：`D:\交易系统\storage`
- DuckDB/Parquet：`D:\交易系统\storage`
- 回测结果：`D:\交易系统\backtest_results`
- 报告输出：`D:\交易系统\reports`

非必要不向 C 盘写入大文件。全局 CLI 工具可以使用系统默认位置，但项目数据、模型缓存、回测缓存和下载数据不得依赖 C 盘空间。

## 角色分工

### 策划者

策划者由主线程承担，负责：

- 维护项目主线：趋势判断 -> 价格行为 -> 量价确认 -> 风控执行 -> 复盘进化。
- 把大任务拆成互不冲突的小任务。
- 给代理写清楚目标、范围、禁止事项、验收标准和返回格式。
- 审核代理报告与代码变更。
- 整合代码与文档。
- 运行完整测试。
- 更新 `项目总规划.md`。

### 研究代理

研究代理只读项目和工具，不改文件。

适合任务：

- 调研 OKX/Binance 能力。
- 设计行情下载、存储、回测、风控、报告架构。
- 分析前瞻偏差、过拟合、成本模型、样本量问题。
- 输出可交给执行代理实现的具体任务。

### 执行代理

执行代理负责小范围代码落地。

要求：

- 必须有明确写入范围。
- 必须知道自己不是唯一在代码库工作的代理。
- 不得回退或覆盖其他代理/用户的改动。
- 必须使用 TDD：先写测试、确认失败、再实现。
- 必须运行指定测试和完整测试。
- 必须返回修改文件、测试结果、实现说明和遗留风险。

## 任务派发格式

每个代理任务必须包含：

- 目标。
- 写入范围或只读范围。
- 必须阅读的上下文文件。
- 禁止事项。
- 验收标准。
- 返回格式。

执行代理返回格式：

```text
STATUS: DONE / DONE_WITH_CONCERNS / BLOCKED
Files changed:
Tests run and results:
Implementation notes:
Risks/concerns:
```

研究代理返回格式：

```text
STATUS: DONE / BLOCKED
Files inspected:
Commands/tools used:
Deep report:
Concrete next tasks:
```

## 审核流程

每轮代理完成后，策划者按以下顺序处理：

1. 阅读每个代理的 `STATUS`。
2. 若 `BLOCKED`，判断是缺上下文、任务过大、环境问题还是方案错误。
3. 对执行代理的变更运行对应测试和完整测试。
4. 检查是否越界修改文件。
5. 把研究报告转化为下一轮可执行任务。
6. 更新必要文档，优先更新 `项目总规划.md`。
7. 清理 `.venv` 外的 `__pycache__` / `.pyc`。
8. 需要时提交 Git。

## 强制规则

- 所有真实交易接口默认关闭。
- 不在聊天中收集 API Secret 或 passphrase。
- 默认只用公开行情，账户相关操作单独隔离。
- 不使用 `1m` 作为主策略周期。
- 新生产代码必须有测试。
- 不混合不同交易所 K 线生成“综合成交价格”。
- 每一轮结束都要能回答：新增了什么、验证了什么、下一步做什么。
