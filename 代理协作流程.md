# 代理协作流程

本项目采用“策划者 + 并行代理”的工作方式推进。主线程承担策划者职责，负责拆任务、审报告、整合代码、继续派发下一轮任务。

## 模型约定

后续默认使用：

- 策划者主线程：`gpt-5.5`，超高推理强度。
- 执行代理：`gpt-5.5`，高推理强度。

只有当任务可以拆成互不冲突的写入范围，或需要独立研究报告时，才派发代理。普通收束、复盘、清理和小型文档更新由策划者直接完成。

## 项目边界

项目根目录为 `D:\交易系统`。

后续项目独有内容优先放在该目录内：

- Python 虚拟环境：`D:\交易系统\.venv`
- 历史行情：`D:\交易系统\data` 或 `D:\交易系统\storage`
- DuckDB/Parquet：`D:\交易系统\storage`
- 回测结果：`D:\交易系统\backtest_results`
- 报告输出：`D:\交易系统\reports`

非必要不向 C 盘写入大文件。全局 CLI 工具可以使用系统默认位置，但项目数据、模型缓存、回测缓存和下载数据不得依赖 C 盘空间。

## 角色分工

### 策划者

策划者负责：

- 维护项目主线：趋势判断 -> 价格行为 -> 量价确认 -> 风控执行 -> 复盘进化。
- 把大任务拆成互不冲突的小任务。
- 给每个代理写清楚目标、范围、禁止事项、验收标准和返回格式。
- 审核代理报告与代码变更。
- 运行完整测试，防止多个代理的工作互相破坏。
- 根据结果继续派发下一轮任务。

### 研究代理

研究代理只读项目和工具，不改文件。

适合任务：

- 调研 OKX CLI / MCP 能力。
- 设计行情下载、存储、回测、风控、报告架构。
- 分析前瞻偏差、过拟合、成本模型、样本量问题。
- 输出可交给 worker 实现的具体任务。

### 执行代理

执行代理负责小范围代码落地。

要求：

- 必须有明确写入范围。
- 必须使用 TDD：先写测试、确认失败、再实现。
- 必须运行指定测试和完整测试。
- 必须返回修改文件、测试结果、实现说明和遗留风险。
- 不得回退或重写其他代理/用户的改动。

## 当前并行任务

### A. 行情/OKX 数据架构研究

目标：设计历史 K 线批量下载和本地存储方案。  
范围：只读研究，不改文件。  
输出：字段设计、增量更新、存储方案、失败重试、下一轮实现任务。

### B. 指标/市场体制实现

目标：实现市场体制指标基础函数。  
写入范围：

- `trading_system/indicators/*`
- `tests/test_regime_indicators.py`

验收：

- 支持 True Range、Kaufman ER、Choppiness Index、体制分类。
- 覆盖趋势、噪音、平盘边界测试。
- 完整测试通过。

### C. 历史行情存储接口实现

目标：实现轻量 K 线仓库接口。  
写入范围：

- `trading_system/data/history.py`
- `tests/test_data_history.py`

验收：

- K 线按时间戳去重、排序。
- 可查询最新时间戳。
- 可从三周期 profiles 提取所需 K 线粒度。
- 完整测试通过。

### D. 回测/风控架构研究

目标：设计第一版 backtest 与 risk 模块。  
范围：只读研究，不改文件。  
输出：模块划分、信号/订单/持仓/成本/交易日志模型、风控规则、前瞻偏差防线、前 5 个实现任务。

## 审核流程

每轮代理完成后，策划者按以下顺序处理：

1. 阅读每个代理的 `STATUS`。
2. 若 `BLOCKED`，判断是缺上下文、任务过大、环境问题还是方案错误。
3. 对 worker 的变更运行对应测试和完整测试。
4. 检查是否越界修改文件。
5. 把研究报告转化为下一轮 worker 任务。
6. 更新 README 或设计文档，让项目状态保持可交接。

## 强制规则

- 所有真实交易接口默认关闭。
- 不在聊天中收集 API Secret 或 passphrase。
- 默认只用公开行情，账户相关操作单独隔离。
- 不使用 `1m` 作为主策略周期。
- 新生产代码必须有测试。
- 每一轮结束都要能回答：新增了什么、验证了什么、下一步做什么。
